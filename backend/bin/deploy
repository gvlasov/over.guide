#!/usr/bin/env bash
set -ex
cd $(git rev-parse --show-toplevel)/backend


tar cfz - config package.json -C build dist -C ../src/database migrations | ssh site@smol.guide 'mkdir -p backend; cd backend; rm -rf config dist; cat | tar xz; ls -lah .'
ssh root@smol.guide 'systemctl restart backend'

