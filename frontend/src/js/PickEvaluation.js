import './PickOption'
import './Pick'

/**
 * @param {PickContext} pickContext
 * @constructor
 */
function PickEvaluation(pickContext) {
    this.otherOptions = [];
}

/**
 * @param {PickContext} pickContext
 */
function buildOptions(pickContext) {
}

export default PickEvaluation;
